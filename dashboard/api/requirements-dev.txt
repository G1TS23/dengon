# Versions épinglées pour la CI (reproductibilité).
# Le pyproject.toml garde des planchers `>=` pour le dev local ; ce fichier fige
# ce que la CI installe. Un vrai lock (uv.lock / pylock.toml) — couvrant aussi
# les dépendances transitives — reste une décision d'équipe (voir la PR).
fastapi==0.115.0
uvicorn[standard]==0.30.6
pytest==8.3.3
httpx==0.27.2
ruff==0.6.9
